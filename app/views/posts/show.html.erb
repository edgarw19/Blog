<div class="container", "relative">
	<div class="row-fluid">
		<h1><%= @post.Title %></h1>

		
		<p>
			<%= @post.Post.html_safe %>
		</p>
		
		<!--must check the comment item-->
		<h3>Comments</h3>
		<%= render @post.comments %>

		<h5>Add a comment:</h5>
		<%= render "comments/form" %>
	</div>
</div>